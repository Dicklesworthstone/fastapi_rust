# Legacy Python source (cloned)
legacy_fastapi/

# Rust build artifacts
/target/

# Local-only cargo config (contains [patch.crates-io] sibling-path overrides
# for dev loops; CI uses the crates.io versions from Cargo.toml).
# See AGENTS.md for details.
.cargo/config.toml

# IDE
.idea/
.vscode/

# OS
.DS_Store
Thumbs.db

# bv (beads viewer) local config and caches
.bv/

# BV lock file (ephemeral)
.beads/.bv.lock

# Source images (webp versions are committed instead)
*.png
._*.png

# Ephemeral/temporary files (agent workflow artifacts)
RESEARCH_FINDINGS.md
TOON_INTEGRATION_BRIEF.md
cov_*.out
rustc-ice-*.txt
*.snap.new
sweep_*.png
test_screenshot.png

# Agent progress tracking files (ephemeral)
claude-upgrade-progress.json

# Compiler output
a.out

# Codex CLI ephemeral directories
.codex-local-check/
.codex-bench/
.codex-tmp/

# ACFS ephemeral patterns
.agent-mail-project-id
.agent-mail.yaml
*.mcp.json
*.mcp.json.*.bak
storage.sqlite3
storage.sqlite3-*
*.sqlite3.bak
core
core.*
